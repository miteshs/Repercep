.venv/
.venv
__pycache__/
*.py[cod]
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.egg-info/
dist/
build/
# Rust workspace artifacts
target/
**/*.rs.bk
# PyO3 extension modules built into per-crate python-source dirs by
# `maturin develop`. Source of truth is the Rust code; the .so is a build
# artifact.
crates/**/python/**/*.so
crates/**/python/**/*.abi3.so
crates/**/python/**/*.pyd
# model weights / large artifacts — never commit
weights/
*.safetensors
*.pt
*.bin
# local runtime
.cache/
*.log
benchmark-results/

# CPU AMX kernel build artifacts
kernels/cpu/*/build/
kernels/cpu/*/*.so

# OS / editor cruft
.DS_Store

# uv lockfile — repo flow is `uv venv` / `uv pip install`, not `uv sync`
uv.lock

# repercep.ai marketing site (published separately, not Mirage source)
site/
*.pdf

