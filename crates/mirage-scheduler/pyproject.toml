# Per-crate pyproject for `maturin develop`.
#
# The Mirage workspace's top-level pyproject.toml describes the pure-Python
# `mirage-runtime` package (hatchling). This file is the maturin-side build
# config for the `mirage_scheduler._native` extension module specifically — it
# does not replace the workspace pyproject, it complements it. `maturin
# develop --manifest-path crates/mirage-scheduler/Cargo.toml` picks this up.

[build-system]
requires = ["maturin>=1.7"]
build-backend = "maturin"

[project]
name = "mirage-scheduler"
version = "0.0.1"
description = "Request scheduler for the Mirage runtime — Rust core with PyO3 bindings."
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [{ name = "Mirage" }]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
]

[tool.maturin]
# Build into the mirage_scheduler._native module so the Python wrapper at
# src/mirage/runtime/scheduler.py can do `from mirage_scheduler._native import ...`.
# `python-source` is the mixed-Python/Rust layout: `python/mirage_scheduler/`
# is the package, `_native` is the PyO3 extension submodule.
module-name = "mirage_scheduler._native"
python-source = "python"
# pyo3 + abi3 — one wheel per Python >= 3.11.
features = ["pyo3/extension-module"]
